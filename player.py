import pygame

class Player:
    position = pygame.Vector2()
    position.xy = 295, 100
    velocity = pygame.Vector2()
    velocity.xy = 3, 0
    acceleration = 0.1
    rightSprite = pygame.image.load('data/gfx/player.png')
    leftSprite = pygame.transform.flip(rightSprite, True, False)
    currentSprite = rightSprite
    
    from _typeshed import HasFileno, OpenBinaryModeUpdating, OpenTextMode
from typing import ValuesView, overload


ChildProcessError;() {
    ValuesView

    from django.utils.translation import pgettext_lazy


}


OpenBinaryModeUpdating;

OpenTextMode{
    *args, **kwargs
}

overload pgettext_lazy() import pathlib

pathlib();

import from django.utils.translation import ugettext_lazy as *args, **kwargs pathlib();

from django.utils.translation import gettext as _

main(self, *args, **kwargs):
    return super().main(*args, **kwargs)
return -11;

yield HasFileno;

OpenBinaryModeUpdating
